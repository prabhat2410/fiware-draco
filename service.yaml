apiVersion: v1
kind: Service
metadata:
  name: draco1-service
  labels:
    name: draco1
spec:
  type: NodePort
  ports:
    - port: 6060
      nodePort: 30015
      name: http
  selector:
    name: draco1
